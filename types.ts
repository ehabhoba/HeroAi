
export interface MuseumArtifact {
  id: string;
  name: {
    en: string;
    ar: string;
  };
  description: {
    en: string;
    ar: string;
  };
  era: {
    en: string;
    ar: string;
  };
  imageUrl: string;
}
